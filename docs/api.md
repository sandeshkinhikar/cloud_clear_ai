# API Reference

## Endpoints

- GET /health: health check
- POST /upload: upload one or more imagery files
- POST /detect-cloud: generate a cloud mask and coverage metrics
- POST /reconstruct: produce a reconstructed image and confidence map
- GET /metrics: return evaluation metrics
- GET /history: return recent inference history
- GET /download: return downloadable output references
