# Utilities

This directory contains reusable support modules for preprocessing, image normalization, and evaluation helpers that can be shared between the backend service and future training pipelines.
